name: tests
on: [push, pull_request]
permissions:
  contents: read
jobs:
  unit:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: '3.10'
      - run: python -m pip install -e '.[dev,direct]' torch --extra-index-url https://download.pytorch.org/whl/cpu
      - run: ruff check src tests
      - run: python -m unittest discover -s tests -v
      - run: python scripts/verify_experiment_data.py
